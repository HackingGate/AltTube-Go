// Code generated by ent, DO NOT EDIT.

package ent

import (
	"AltTube-Go/ent/alttube"
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// AltTube is the model entity for the AltTube schema.
type AltTube struct {
	config
	// ID of the ent.
	ID           int `json:"id,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AltTube) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case alttube.FieldID:
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AltTube fields.
func (at *AltTube) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case alttube.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			at.ID = int(value.Int64)
		default:
			at.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the AltTube.
// This includes values selected through modifiers, order, etc.
func (at *AltTube) Value(name string) (ent.Value, error) {
	return at.selectValues.Get(name)
}

// Update returns a builder for updating this AltTube.
// Note that you need to call AltTube.Unwrap() before calling this method if this AltTube
// was returned from a transaction, and the transaction was committed or rolled back.
func (at *AltTube) Update() *AltTubeUpdateOne {
	return NewAltTubeClient(at.config).UpdateOne(at)
}

// Unwrap unwraps the AltTube entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (at *AltTube) Unwrap() *AltTube {
	_tx, ok := at.config.driver.(*txDriver)
	if !ok {
		panic("ent: AltTube is not a transactional entity")
	}
	at.config.driver = _tx.drv
	return at
}

// String implements the fmt.Stringer.
func (at *AltTube) String() string {
	var builder strings.Builder
	builder.WriteString("AltTube(")
	builder.WriteString(fmt.Sprintf("id=%v", at.ID))
	builder.WriteByte(')')
	return builder.String()
}

// AltTubes is a parsable slice of AltTube.
type AltTubes []*AltTube
